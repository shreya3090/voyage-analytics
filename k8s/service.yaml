apiVersion: v1
kind: Service
metadata:
  name: voyage-api-service
spec:
  type: NodePort
  selector:
    app: voyage-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007